{
  "name": "tooti",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "mono-run dev --stream 500 -ui row"
  },
  "devDependencies": {
    "monorepo-run": "^0.4.2"
  }
}
